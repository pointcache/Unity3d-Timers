using UnityEngine;
using System.Collections;
using USS.Timers;
public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Timer.Repeater(5f, null);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
